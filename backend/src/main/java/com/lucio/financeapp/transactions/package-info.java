@ApplicationModule(displayName = "Transactions", allowedDependencies = { "shared", "shared::domain",
        "users::security" })
package com.lucio.financeapp.transactions;

import org.springframework.modulith.ApplicationModule;
